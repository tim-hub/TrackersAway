{
  "name": "clean-hosts-node",
  "version": "0.1.1",
  "description": "The best cross-platform hosts manager.",
  "main": "src/main/main.js",
  "scripts": {
    "test": "find ./src -name '*.test.js' | xargs mocha -R spec",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "electron src/main/main.js",
    "build:mac": "PRODUCTION=true electron-packager . hosts --platform=darwin --arch=x64 --prune=true --out=dist --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "",
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "axios": "^0.19.0",
    "bulma-tooltip": "^2.0.2",
    "electron": "^5.0.7",
    "electron-installer-dmg": "^3.0.0",
    "electron-packager": "^14.0.5",
    "electron-reload": "^1.4.1",
    "mocha": "^6.2.0",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3",
    "redux": "^4.0.4",
    "sudo-prompt": "^9.0.0"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "pino": "^5.13.1",
    "pino-pretty": "^3.2.0"
  }
}
